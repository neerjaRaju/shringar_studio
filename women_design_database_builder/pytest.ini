[pytest]
# Ensures the `builder` package is importable when tests are run via the
# bare `pytest` console script (which, unlike `python -m pytest`, does not
# add the current directory to sys.path).
pythonpath = .
testpaths = tests
